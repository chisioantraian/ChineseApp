<Window x:Class="WpfApp2.MainWindow"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:WpfApp2"
        mc:Ignorable="d"
        Title="MainWindow" Height="1080" Width="1920">

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="*" />
            <RowDefinition Height="8*" />
        </Grid.RowDefinitions>



        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="8*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <Button
                Name="RandomButton"
                Click="RandomButton_Click"
                Grid.Column="0" 
                Content="Random"
                FontSize="20"
                Margin="15"/>
            
            <TextBox 
                Grid.Column="1"
                Name="SearchBar" FontSize="20" 
                Padding="15" Margin="15" 
                KeyUp="SearchBar_KeyUp"/>

            <Button 
                Name="SearchButton"
                Click="SearchButton_Click"
                Grid.Column="2" 
                Content="Search" 
                FontSize="20"
                Margin="15"/>

            <ComboBox 
                Name="InputComboBox" 
                Grid.Column="3"
                Margin="15"
                Padding="15"
                FontSize="16"
                SelectedIndex="0">
                <ComboBoxItem>English</ComboBoxItem>
                <ComboBoxItem>Chinese</ComboBoxItem>
                <ComboBoxItem>Pronounciation</ComboBoxItem>
                <ComboBoxItem>Compose</ComboBoxItem>
            </ComboBox>
            

        </Grid>

        <Grid Grid.Row="1">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="*" />
            </Grid.ColumnDefinitions>

            <ListBox Name="WordsList" 
                     Grid.Column="0" 
                     VirtualizingStackPanel.VirtualizationMode="Recycling"
                     HorizontalContentAlignment="Stretch">
                
                <TextBlock Name="ResultCountBlock"
                       Margin="10"
                       FontSize="24" />
            </ListBox>

            <ListBox Name="ZoomedWordBox"
                     ScrollViewer.CanContentScroll="False"
                     Grid.Column="1">

            </ListBox>


            <ListBox Name="ExamplesList"
                     Grid.Column="2" >
                <StackPanel Name="StatisticsBox">
                    <TextBlock Name="DecompositionBlock"
                           FontSize="20"
                           Margin="10" />
                </StackPanel>
            </ListBox>
        </Grid>
    </Grid>
    
</Window>
